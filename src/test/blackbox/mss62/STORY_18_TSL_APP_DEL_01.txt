TEST CASE: TSL_APP_DEL_01
METHOD: deleteProfileHelper()
DESCRIPTION: Ensures that the current applicant’s profile is removed from the user list and current user is set to null.

PRECONDITIONS:
  - The system has an Applicant user with ID = "A100".
  - This applicant is set as the current user in the system.
  - The global user list contains that Applicant.

ACTIONS:
  1. Invoke deleteProfileHelper() with no additional input

EXPECTED OUTCOMES:
  - The applicant with ID "A100" is removed from the global user list.
  - The system’s current user reference is reset to null.
  - No error messages are displayed in the process.