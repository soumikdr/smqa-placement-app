TEST CASE: TSL_REC_RSTPWD_01
METHOD: viewResetPasswordPage()
DESCRIPTION: Ensures that if the user chooses not to reset the password (typing 'n'), the system redirects to the recruiter dashboard.

PRECONDITIONS:
  - The current user is a Recruiter with username = "RecruiterX".
  - The system is on the "Reset password?" prompt.

ACTIONS:
  1. The user is prompted "Do you want to reset your password? (y/n)" => user types "n".
  2. viewResetPasswordPage() interprets that choice.

EXPECTED OUTCOMES:
  - The system prints "Redirecting to dashboard".
  - Control flows back to the Recruiter Dashboard screen.
  - No password reset flow is triggered.